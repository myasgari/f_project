#include <iostream>

using namespace std;
enum choise { CREATE = 1, ERASE, ERASEALL, LIST, EXPORT };
void create(vector<shape *> &);
void list(vector<shape *>&);
int enterChois();
int main()
{

	return 0;
}
